<UserControl x:Class="WheatGrainClassifierWpfApp.Views.CalculationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WheatGrainClassifierWpfApp.Views"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>
        
        <!-- Gauche -->
        <Grid Background="white" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TRAIN -->
            <Grid Grid.Row="0" Background="white"  Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel   Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial Kind="Database" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="#1F6FEB"/>
                    <TextBlock Text="Données d’apprentissage (train)" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="1" Command="{Binding LoadTrainCommand}" Padding="14,6"
                    Margin="10,0,0,0"
                    VerticalAlignment="Top"
                    BorderThickness="1.5"
                    BorderBrush="#3B82F6"
                    Foreground="#3B82F6"
                    Background="Transparent"
                    Cursor="Hand">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                        <!-- Folder Icon -->
                        <iconPacks:PackIconMaterial Kind="FolderUpload" Width="16" Height="16" Margin="0,0,6,0" Foreground="#3B82F6"/>

                        <TextBlock Text="Charger Train"
                       VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>
            

            <DataGrid Grid.Row="1" ItemsSource="{Binding TrainData}" AutoGenerateColumns="False" IsReadOnly="True">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Label"     Binding="{Binding Variety}"       Width="*"/>
                    <DataGridTextColumn Header="Surface"    Binding="{Binding Area}"        Width="*"/>
                    <DataGridTextColumn Header="Périmètre"  Binding="{Binding Perimeter}"   Width="*"/>
                    <DataGridTextColumn Header="Compacité"  Binding="{Binding Compactness}" Width="*"/>
                    <DataGridTextColumn Header="Longueur"   Binding="{Binding Kernel_Length}" Width="*"/>
                    <DataGridTextColumn Header="Largeur"    Binding="{Binding Kernel_Width}" Width="*"/>
                    <DataGridTextColumn Header="Asymétrie"  Binding="{Binding Asymmetry_Coefficient}" Width="*"/>
                    <DataGridTextColumn Header="Sillon"     Binding="{Binding Groove_Length}" Width="*"/>
                </DataGrid.Columns>

            </DataGrid>

            <!-- TEST -->
            <Grid Grid.Row="2" Background="white"  Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel   Orientation="Horizontal" VerticalAlignment="Center">
                    <iconPacks:PackIconMaterial Kind="Database" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="#1F6FEB"/>
                    <TextBlock Text="Données de test" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Grid.Column="1" Command="{Binding LoadTestCommand}" Padding="14,6"
                    Margin="10,0,0,0"
                    VerticalAlignment="Top"
                    BorderThickness="1.5"
                    BorderBrush="#3B82F6"
                    Foreground="#3B82F6"
                    Background="Transparent"
                    Cursor="Hand">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                        <!-- Folder Icon -->
                        <iconPacks:PackIconMaterial Kind="FolderUpload"
                            Width="16"
                            Height="16"
                            Margin="0,0,6,0"
                            Foreground="#3B82F6"/>

                        <TextBlock Text="Charger Test" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </Grid>

            <DataGrid Grid.Row="3" ItemsSource="{Binding TestData}" AutoGenerateColumns="False" IsReadOnly="True">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Label"     Binding="{Binding Variety}"       Width="*"/>
                    <DataGridTextColumn Header="Surface"    Binding="{Binding Area}"        Width="*"/>
                    <DataGridTextColumn Header="Périmètre"  Binding="{Binding Perimeter}"   Width="*"/>
                    <DataGridTextColumn Header="Compacité"  Binding="{Binding Compactness}" Width="*"/>
                    <DataGridTextColumn Header="Longueur"   Binding="{Binding Kernel_Length}" Width="*"/>
                    <DataGridTextColumn Header="Largeur"    Binding="{Binding Kernel_Width}" Width="*"/>
                    <DataGridTextColumn Header="Asymétrie"  Binding="{Binding Asymmetry_Coefficient}" Width="*"/>
                    <DataGridTextColumn Header="Sillon"     Binding="{Binding Groove_Length}" Width="*"/>
                </DataGrid.Columns>

            </DataGrid>
        </Grid>
        
        <!-- Droite -->
        <StackPanel  Grid.Column="1" Margin="15">

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <iconPacks:PackIconMaterial Kind="Cog" Foreground="#1F6FEB"  VerticalAlignment="Center"/>
                <TextBlock Text="Configuration du classifieur" FontSize="16"  VerticalAlignment="Center" FontWeight="SemiBold" Margin="5,0"/>
            </StackPanel>

            <!-- K -->
            <TextBlock Text="Valeur de k :" Margin="0,10,0,0"/>
            <TextBox Text="{Binding K, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

            <!-- DISTANCE -->
            <TextBlock Text="Distance" Margin="0,10,0,0"/>
            <ComboBox ItemsSource="{Binding Distances}" SelectedItem="{Binding SelectedDistance}" Margin="0,5"/>

            <!-- BUTTON RUN-->
            <Button Command="{Binding RunCommand}" Padding="3" Margin="0,15,0,15" 
                    Background="#2563EB" Foreground="White" Cursor="Hand">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <iconPacks:PackIconMaterial Kind="Play"  VerticalAlignment="Center"/>
                    <TextBlock Text="Lancer la classification" Margin="5,0"  VerticalAlignment="Center"/>
                </StackPanel>
            </Button>


            <StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <iconPacks:PackIconMaterial Kind="ChartBar" Foreground="#1F6FEB" Width="20" Height="20"/>
                    <TextBlock Text="Résultats" FontWeight="Bold" FontSize="18" Margin="5,0"/>
                </StackPanel>

                <!-- Exactitude -->
                <TextBlock Text="Exactitude : "  Margin="0,10,0,0"/>
                <TextBlock Text="{Binding Exactitude, StringFormat='{}{0:F2}%'}" FontSize="18" FontWeight="Bold" Foreground="Green"/>

                <!-- CONFUSION MATRIX -->
                <TextBlock Text="Matrice de confusion" Margin="0,15,0,5"/>
                <DataGrid ItemsSource="{Binding ConfusionMatrix}" Padding="3" AutoGenerateColumns="False" Height="115">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Actuel \\ Prédit" Binding="{Binding Variety}" Width="*"/>
                        <DataGridTextColumn Header="Canadian" Binding="{Binding Canadian}" Width="*"/>
                        <DataGridTextColumn Header="Kama" Binding="{Binding Kama}" Width="*"/>
                        <DataGridTextColumn Header="Rosa" Binding="{Binding Rosa}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>

            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
