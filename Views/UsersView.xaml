<UserControl x:Class="WheatGrainClassifierWpfApp.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <iconPacks:PackIconMaterial Kind="AccountGroup" Width="24" Height="24"
                                       Foreground="#1F6FEB" VerticalAlignment="Center"/>
            <TextBlock Text="Utilisateurs — API dummyjson.com"
                       FontSize="20" FontWeight="SemiBold"
                       Margin="8,0,0,0" VerticalAlignment="Center"/>
        </StackPanel>

       
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Command="{Binding LoadUsersCommand}"
                    Padding="14,6" BorderThickness="1.5"
                    BorderBrush="#3B82F6" Foreground="#3B82F6"
                    Background="Transparent" Cursor="Hand">
                <StackPanel Orientation="Horizontal">
                    <iconPacks:PackIconMaterial Kind="CloudDownload" Width="16" Height="16"
                                               Margin="0,0,6,0" Foreground="#3B82F6"/>
                    <TextBlock Text="Charger les utilisateurs" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
            <TextBlock Grid.Column="1" Text="{Binding StatusMessage}"
                       Foreground="#555" FontStyle="Italic"
                       Margin="15,0,0,0" VerticalAlignment="Center"/>
        </Grid>

        
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Sélectionné : " FontWeight="SemiBold"/>
            <TextBlock Text="{Binding SelectedUser.FullName, FallbackValue='Aucun'}"
                       Foreground="#1F6FEB" FontWeight="SemiBold"/>
        </StackPanel>

       
        <ListView Grid.Row="3" ItemsSource="{Binding Users}" SelectedItem="{Binding SelectedUser}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="ID"       DisplayMemberBinding="{Binding Id}"        Width="50"/>
                    <GridViewColumn Header="Prénom"   DisplayMemberBinding="{Binding FirstName}"  Width="130"/>
                    <GridViewColumn Header="Nom"      DisplayMemberBinding="{Binding LastName}"   Width="130"/>
                    <GridViewColumn Header="Courriel" DisplayMemberBinding="{Binding Email}"      Width="250"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
